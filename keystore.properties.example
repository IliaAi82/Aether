# Copy to keystore.properties (repo root) and fill in. NEVER commit the real file.
# Create the keystore itself with:  bash scripts/generate-keystore.sh
storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=aether-release
keyPassword=CHANGE_ME
